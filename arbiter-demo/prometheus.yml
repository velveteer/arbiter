# Deployed-demo scrape config: arbiter is a compose service here, not a host process.
global:
  scrape_interval: 10s

scrape_configs:
  - job_name: arbiter
    static_configs:
      - targets: ["demo:9464"]
